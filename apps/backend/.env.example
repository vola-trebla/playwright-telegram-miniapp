# Copy to .env. The bot token signs/validates Mini App initData (get it from @BotFather).
BOT_TOKEN=
PORT=3000
# DEV_MODE=true lets you open the Mini App in a plain browser (no Telegram).
# Set DEV_MODE=false to enforce real initData validation once the bot is wired up.
DEV_MODE=true
